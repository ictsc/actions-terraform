# actions-terraform

terraformリポジトリに対してよくやるステップをまとめたaction。

以下を実行します。
1. `terraform fmt`
1. `terraform init`
1. `terraform validate`
1. `terraform plan`
1. `terraform apply` (only master branch push)
